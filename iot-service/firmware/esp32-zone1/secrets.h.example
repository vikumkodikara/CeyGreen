#pragma once

// Copy this file to secrets.h and fill in real values.
// secrets.h is gitignored — never commit WiFi passwords.

#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// PC / server IP reachable from ESP32 (not localhost)
#define IOT_BASE_URL "http://192.168.1.10:8082"

#define API_KEY "ceygreen-dev-api-key"
#define GREENHOUSE_ID "GH001"
#define ZONE_ID "ZONE1"
